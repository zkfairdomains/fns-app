import React from "react";
import { Outlet } from "react-router-dom";  

const Page = () => { 
  return (
    <> 
      <div className="container-fluid">
        Terms
      </div>
    </>
  )
};

export default Page;